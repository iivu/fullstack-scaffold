{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && tsdown && node scripts/format-package-json.js",
    "start": "node dist/index.mjs",
    "lint": "biome lint",
    "check": "biome check",
    "check-write": "biome check --write",
    "check-types": "tsc --noEmit",
    "db:migrate:push": "drizzle-kit push",
    "db:migrate:gen": "drizzle-kit generate",
    "db:migrate:run": "drizzle-kit migrate",
    "db:seed": "tsx src/db/seeder.ts"
  },
  "dependencies": {
    "@hono/node-server": "catalog:",
    "@hono/zod-validator": "catalog:",
    "@r/contracts": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "mysql2": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "tsdown": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}